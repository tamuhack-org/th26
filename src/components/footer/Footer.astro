---
import FooterTransition from '@assets/footer/footer-transition.svg';
import { waveEnd, sealEnd, catEnd } from '@assets/footer/footer-end-path'; // End svg paths of wave, seal, and cat assets
import LinkSection from './LinkSection.astro';
import {
	resources,
	socials,
	otherHackathons,
	workshops,
} from './footer-links.js';
---

<div
	id="footer-container"
	data-wave-end={waveEnd}
	data-seal-end={sealEnd}
	data-cat-end={catEnd}
>
	<FooterTransition aria-hidden={true} />
	<script>
		import { gsap } from 'gsap';
		import { ScrollTrigger } from 'gsap/ScrollTrigger';

		gsap.registerPlugin(ScrollTrigger);

		const el = document.querySelector('#footer-container');
		const waveEnd = (el as HTMLElement).dataset.waveEnd!;
		const sealEnd = (el as HTMLElement).dataset.sealEnd!;
		const catEnd = (el as HTMLElement).dataset.catEnd!;

		let mm = gsap.matchMedia(),
			breakpoint = 40; // in rem

		mm.add(
			{
				reduceMotion: '(prefers-reduced-motion: reduce)',
				isMobile: `(max-width: ${breakpoint}rem)`,
				isDesktop: `(min-width: ${breakpoint}rem)`,
			},
			(context) => {
				let { reduceMotion, isMobile, isDesktop } =
					context.conditions as any;

				if (reduceMotion) return;

				const mainTimeline = gsap.timeline({
					scrollTrigger: {
						trigger: '#footer-container',
						start: 'top 85%',
						end: isDesktop ? 'bottom bottom' : '50% bottom',
						scrub: 1,
					},
				});

				mainTimeline
					.to('.animate-wave', { attr: { d: waveEnd } })
					.to('.animate-seal', { attr: { d: sealEnd } }, '<')
					.to('.animate-cat', { attr: { d: catEnd } }, '<');
			},
		);
	</script>
	<div class="-mt-1 bg-[#3455c3] px-8 py-4 text-white lg:px-16">
		<div class="mx-auto max-w-[2000px]">
			<footer class="items-center lg:flex lg:justify-between">
				<div class="text-center opacity-90 lg:text-start">
					<p class="mb-0 text-2xl font-semibold">
						Sign up to our newsletter
					</p>
					<p class="font-light">
						Stay connected! Sign up for our mailing list for events,
						updates, and more.
					</p>
				</div>
				<form
					action="https://tamuhack.us21.list-manage.com/subscribe?u=6916895bdf09ac34cd4e523b9&id=669dbb893c"
					method="post"
					id="mc-embedded-subscribe-form"
					name="mc-embedded-subscribe-form"
					target="_blank"
					novalidate
					class="flex flex-col items-center justify-start gap-2 lg:flex-row lg:items-stretch"
				>
					<div
						class="flex w-full flex-row items-stretch justify-center gap-3 px-4 py-4 max-[27.5rem]:flex-col max-[27.5rem]:justify-stretch lg:flex-none lg:gap-5 lg:px-0"
					>
						<label for="mce-EMAIL" class="hidden"
							>Email Address</label
						>
						<input
							type="text"
							name="EMAIL"
							placeholder="Enter your email address"
							class="focus:outline-red w-64 rounded-lg border border-white px-4 py-2 max-[27.5rem]:w-full 2xl:w-96"
							id="mce-EMAIL"
						/>
						<button class="clear">
							<input
								type="submit"
								value="Subscribe"
								name="subscribe"
								id="mc-embedded-subscribe"
								class="h-full cursor-pointer rounded-lg border border-white px-4 py-2 text-base font-bold transition-colors duration-200 hover:bg-[#e3b8dd] hover:text-[#1b0000]"
							/>
						</button>
					</div>
					<!-- Mailchip bot prevention -->
					<div aria-hidden="true" class="absolute -left-[5000px]">
						<input
							type="text"
							name="b_43a795784ca963e25903a0da6_9937fe4fc5"
							tabindex="-1"
							value=""
						/>
					</div>
				</form>
			</footer>

			<footer
				class="text-dark my-8 justify-between opacity-90 lg:my-16 xl:flex"
			>
				<div class="xl:w-1/3">
					<p
						class="font-family-bbh mb-8 text-3xl font-bold tracking-wider"
					>
						TAMUhack 2026
					</p>
					<p class="font-base mb-8 text-xl">
						Website brought to you by the TAMUhack Design +
						Development Teams
					</p>
				</div>
				<div
					class="flex grow flex-wrap justify-start gap-12 lg:ml-12 lg:justify-end"
				>
					<LinkSection title="Hacker Resources" items={resources} />
					<LinkSection title="Socials" items={socials} />
					<LinkSection
						title="Other Hackathons"
						items={otherHackathons}
					/>
					<LinkSection title="Workshops" items={workshops} />
				</div>
			</footer>

			<footer class="bottom-0">
				<div class="bg-dark bg-opacity-5 mx-8 h-[3px] rounded-xl"></div>
				<p class="text-dark py-3 text-sm font-light">
					Interested in sponsoring? Reach out to us at{' '}
					<a class="underline" href="mailto:sponsor@tamuhack.com">
						sponsor@tamuhack.com
					</a>.
				</p>
			</footer>
		</div>
	</div>
</div>
